# Glance environment configuration
# Copy to app.env and customize before starting
# Main configuration is done via volumes/config/glance/glance.yml

# Timezone
TZ=America/New_York

# Hostname / domain (used in config.yml if needed)
BASE_HOST_NAME=glance.example.com
BASE_DOMAIN_NAME=example.com
